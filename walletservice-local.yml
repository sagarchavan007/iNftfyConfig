logging:
    file:
        name: /tmp/walletservice.log
management:
    endpoint:
        health:
            enabled: true
            probes:
                enabled: true
    endpoints:
        web:
            exposure:
                include: health,info
spring:
    security:
        oauth2:
            resourceserver:
                jwt:
                    issuer-uri: https://auth.cognologix.in/realms/iNFTFy
                    jwk-set-uri: https://auth.cognologix.in/realms/iNFTFy/protocol/openid-connect/certs
    datasource:
        url: jdbc:postgresql://localhost:5432/inftfy
        username: postgres
        password: postgres
    jpa:
        hibernate:
            ddl-auto: update
    jackson:
        serialization:
            fail-on-empty-beans: false
app:
    config:
        keycloak:
            server-url: https://auth.cognologix.in/
            realm: iNFTFy
            clientId: adminclient
            client-secret: CthnmP04Myvqgsi8adYpvbK8qQQ6sPqS
            ssl-required: none